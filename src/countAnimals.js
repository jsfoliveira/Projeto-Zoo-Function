const data = require('../data/zoo_data');

function countAnimals(animal) {
// const { species } = data;
// const contar = species.map((element) => `${element.name}: ${element.residents.length}`);
}

// console.log(countAnimals({ specie: 'penguins' }));

module.exports = countAnimals;
